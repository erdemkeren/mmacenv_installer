#!/usr/bin/env bash

alias art="artisan"
alias tinker="artisan tinker"
alias serve="artisan serve"

## Git
alias gs="git status"
alias gd="git diff"
alias gaa="git add ."
alias gc="git commit"
alias gec="git checkout"
alias göm="git merge"
alias gl="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias nah="git reset --hard && git clean -df"

## Vagrant
alias sun="cd ~/dev/Homestead"
alias vm="sun && vagrant ssh"
alias svm="sun && vagrant up"
alias hvm="sun && vagrant halt"
alias dvm="sun && vagrant destroy"
alias provm="sun && vagrant provision"

## Homestead
alias edit-homestead="atom ~/dev/Homestead/Homestead.yaml"

## devfsh
alias c="clear"
alias cdc="cd ~/dev/code"
alias cdco="cd ~/dev/code/opencode"
alias cdcw="cd ~/dev/code/workcode"
alias cdcp="cd ~/dev/code/percode"
alias cdcpro="cd ~/dev/code/procode"

## aliases
alias edit-aliases="atom ~/.aliases"
